import { status, type } from "../utils/enums/statusEnum";
export const repair ={ status: status.APPROVED, type: type.REAPIR }
export const replace ={ status: status.APPROVED, type: type.REPLACE }

export const approved={ status: status.APPROVED}
export const rejected={ status: status.REJECTED }